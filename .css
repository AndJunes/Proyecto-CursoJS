.distancia-texto.caliente {
  color: orange;
}

.distancia-texto.muy-caliente {
  color: red;
  font-weight: bold;
}

.distancia-texto.trampa {
  color: #d00000;
  animation: shake 0.3s;
  font-weight: bold;
}

@keyframes shake {
  0% { transform: translateX(0); }
  25% { transform: translateX(-5px); }
  50% { transform: translateX(5px); }
  75% { transform: translateX(-5px); }
  100% { transform: translateX(0); }
}
